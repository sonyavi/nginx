# Быстрая настройка GitLab CI/CD переменных

## Пошаговая инструкция

### 1. Откройте настройки проекта в GitLab

1. Перейдите в ваш проект в GitLab
2. Нажмите **Settings** (в левом меню)
3. Выберите **CI/CD** → **Variables** → **Expand**

### 2. Добавьте обязательные переменные

Нажмите **Add variable** для каждой переменной:

#### ✅ ANSIBLE_HOST
- **Key:** `ANSIBLE_HOST`
- **Value:** `192.168.64.8` (IP вашей виртуальной машины)
- **Type:** Variable
- **Protected:** ❌ (можно оставить)
- **Masked:** ❌

#### ✅ ANSIBLE_USER
- **Key:** `ANSIBLE_USER`
- **Value:** `sonyavi` (ваш пользователь на VM)
- **Type:** Variable
- **Protected:** ❌
- **Masked:** ❌

### 3. Выберите способ аутентификации

#### Вариант A: SSH ключ (рекомендуется) 🔐

##### SSH_PRIVATE_KEY
- **Key:** `SSH_PRIVATE_KEY`
- **Value:** 
  ```bash
  # Получите содержимое вашего приватного ключа:
  cat ~/.ssh/id_rsa
  # Или если используете другой ключ:
  cat ~/.ssh/id_ed25519
  ```
  Скопируйте **весь** ключ, включая строки:
  ```
  -----BEGIN OPENSSH PRIVATE KEY-----
  ...
  -----END OPENSSH PRIVATE KEY-----
  ```
- **Type:** Variable
- **Protected:** ✅ (включите!)
- **Masked:** ❌ (нельзя маскировать многострочные значения)

**Важно:** Убедитесь, что публичный ключ добавлен на виртуальную машину:
```bash
# На вашей локальной машине:
ssh-copy-id sonyavi@192.168.64.8

# Или вручную:
cat ~/.ssh/id_rsa.pub | ssh sonyavi@192.168.64.8 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

#### Вариант B: Пароли (менее безопасно) ⚠️

Если SSH ключ недоступен, используйте пароли:

##### ANSIBLE_SSH_PASS
- **Key:** `ANSIBLE_SSH_PASS`
- **Value:** `!713Wh3u` (ваш SSH пароль)
- **Type:** Variable
- **Protected:** ✅ (включите!)
- **Masked:** ✅ (включите!)

##### ANSIBLE_BECOME_PASS
- **Key:** `ANSIBLE_BECOME_PASS`
- **Value:** `!713Wh3u` (ваш sudo пароль)
- **Type:** Variable
- **Protected:** ✅ (включите!)
- **Masked:** ✅ (включите!)

### 4. Опциональные переменные

#### ANSIBLE_HOST_NAME (необязательно)
- **Key:** `ANSIBLE_HOST_NAME`
- **Value:** `vm-nginx` (имя хоста в inventory, по умолчанию уже `vm-nginx`)
- **Type:** Variable
- **Protected:** ❌
- **Masked:** ❌

## Минимальная конфигурация (SSH ключ)

Для работы достаточно добавить эти 3 переменные:

```
✅ ANSIBLE_HOST = 192.168.64.8
✅ ANSIBLE_USER = sonyavi
✅ SSH_PRIVATE_KEY = -----BEGIN OPENSSH PRIVATE KEY-----
                     ...
                     -----END OPENSSH PRIVATE KEY-----
```

## Минимальная конфигурация (пароли)

Если используете пароли:

```
✅ ANSIBLE_HOST = 192.168.64.8
✅ ANSIBLE_USER = sonyavi
✅ ANSIBLE_SSH_PASS = !713Wh3u
✅ ANSIBLE_BECOME_PASS = !713Wh3u
```

## Проверка настройки

После добавления переменных:

1. Перейдите в **CI/CD → Pipelines**
2. Нажмите **Run pipeline**
3. Выберите ветку (например, `main`)
4. Нажмите **Run pipeline**
5. Дождитесь выполнения job `validate` - должен пройти успешно ✅

## Безопасность

### ✅ Рекомендации:

1. **Используйте SSH ключи** вместо паролей
2. **Включите Protected** для всех переменных с секретами
3. **Включите Masked** для паролей (если возможно)
4. **Не коммитьте** пароли и ключи в репозиторий
5. **Регулярно ротируйте** ключи и пароли

### ⚠️ Важно:

- Переменные с `Protected: ✅` доступны только для protected веток (обычно main/master)
- Переменные с `Masked: ✅` скрыты в логах CI/CD
- SSH ключи нельзя маскировать (они многострочные)

## Troubleshooting

### Проблема: "Permission denied (publickey)"

**Решение:**
1. Проверьте, что `SSH_PRIVATE_KEY` содержит полный ключ (включая BEGIN/END строки)
2. Убедитесь, что публичный ключ добавлен на VM:
   ```bash
   ssh sonyavi@192.168.64.8 "cat ~/.ssh/authorized_keys"
   ```
3. Проверьте права на VM:
   ```bash
   ssh sonyavi@192.168.64.8 "chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys"
   ```

### Проблема: "sudo: a password is required"

**Решение:**
1. Убедитесь, что `ANSIBLE_BECOME_PASS` установлен
2. Или настройте NOPASSWD на VM:
   ```bash
   echo "sonyavi ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/sonyavi
   ```

### Проблема: "Host key verification failed"

**Решение:**
- Это уже обработано в CI/CD (используется `ssh-keyscan`)
- Если проблема сохраняется, проверьте доступность хоста

## Где найти переменные в GitLab

**Путь:** 
```
Проект → Settings → CI/CD → Variables → Expand → Add variable
```

**Или прямая ссылка:**
```
https://gitlab.com/your-group/your-project/-/settings/ci_cd
```

## Готово! 🎉

После настройки переменных вы можете:
- Автоматически проверять синтаксис при каждом push
- Вручную запускать развертывание через GitLab CI/CD
- Безопасно хранить учетные данные в GitLab

